FROM --platform=linux/amd64 electronuserland/builder:wine

# Pin Node.js 18 (required for Win7 legacy Electron 22 line)
RUN npm install -g n && n 18 && hash -r && node --version

COPY docker/build-win7.sh /build.sh
RUN chmod +x /build.sh

WORKDIR /project
ENTRYPOINT ["/bin/bash", "/build.sh"]